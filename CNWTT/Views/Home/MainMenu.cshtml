@using CNWTT.App_Start;
@using CNWTT.Core.User;
@{
    Layout = null;
    IList<manufacturer> manu = null;
}

@{ 
    manu = ViewBag.listManufacturer;
}

<div class="header_slide">
    <div class="header_bottom_left ">
        <div class="categories">
            <ul class="tooltip">
                <h3>@CONFIG.PRODUCT_CONTENT</h3>
                <li><a href="@Url.Action("ViewProduct", "Home", new { type = "all" })">@CONFIG.PRODUCT_CONTENT_ALL</a></li>    
                <li>
                    <a href="@Url.Action("ViewProduct", "Home", new { type = "mobile" })">@CONFIG.PRODUCT_CONTENT_MOBILE</a>
                      
                    <div class="tooltiptext">
                        <h3>Hãng sản xuất</h3>
                        <ul class="col_1_of_3">
                            
                            @if (manu != null)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i].name;                            
                                        url = Url.Action("ViewProduct", "Home",new { type="mobile",  manu = manu[i].id});
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {

                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 4].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "mobile", manu = manu[i+4].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                           
                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 8].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "mobile", manu = manu[i + 8].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                </div>
                </li>
                <li>
                    <a href="@Url.Action("ViewProduct", "Home", new { type = "tablet"})">@CONFIG.PRODUCT_CONTENT_TABLET</a>
                    <div class="tooltiptext">
                        <h3>Hãng sản xuất</h3>
                        <ul class="col_1_of_3">

                            @if (manu != null)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "tablet", manu = manu[i].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {

                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 4].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "tablet", manu = manu[i + 4].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }

                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 8].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "tablet", manu = manu[i + 8].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                    </div>
                </li>
                <li><a href="@Url.Action("ViewProduct", "Home", new { type = "laptop"})">@CONFIG.PRODUCT_CONTENT_LAPTOP</a>
                    <div class="tooltiptext">
                        <h3>Hãng sản xuất</h3>
                        <ul class="col_1_of_3">

                            @if (manu != null)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "laptop", manu = manu[i].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {

                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 4].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "laptop", manu = manu[i + 4].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }

                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 8].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "laptop", manu = manu[i + 8].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                    </div>
                </li>
                <li><a href="@Url.Action("ViewProduct", "Home", new { type = "desktop"})">@CONFIG.PRODUCT_CONTENT_DESKTOP</a>
                    <div class="tooltiptext">
                        <h3>Hãng sản xuất</h3>
                        <ul class="col_1_of_3">

                            @if (manu != null)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "desktop", manu = manu[i].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {

                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 4].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "desktop", manu = manu[i + 4].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }

                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 8].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "desktop", manu = manu[i + 8].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                    </div>
                </li>
                <li><a href="@Url.Action("ViewProduct", "Home", new { type = "accessories"})">@CONFIG.PRODUCT_CONTENT_ACCESSORY</a>
                    <div class="tooltiptext">
                        <h3>Hãng sản xuất</h3>
                        <ul class="col_1_of_3">

                            @if (manu != null)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "accessories", manu = manu[i].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {

                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 4].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "accessories", manu = manu[i + 4].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }

                        </ul>
                        <ul class="col_1_of_3">
                            @if (manu != null && manu.Count > 4)
                            {
                                for (int i = 0; i < 4; i++)
                                {
                                    string name = "";
                                    string url = "";
                                    try
                                    {
                                        name = manu[i + 8].name;
                                        url = Url.Action("ViewProduct", "Home", new { type = "accessories", manu = manu[i + 8].id });
                                    }
                                    catch (ArgumentOutOfRangeException e)
                                    {
                                        break;
                                    }
                                    <li><a href="@url">@name</a></li>
                                }

                            }
                        </ul>
                    </div>
                </li>
            </ul>
        </div>

        <div class="categories">
            <ul class="tooltip">
                <h3>Chi tiết liên hệ</h3>
                <li>
                    <a href="#">Mobile</a>
                    <!--
                    <span class="tooltiptext">
                        <ul>
                            <li><a href="#">Máy tính để bàn</a></li>
                            <li><a href="#">Máy </a></li>
                            <li><a href="#">Máy in</a></li>
                            <li><a href="#">Loa</a></li>
                            <li><a href="#">Phụ kiện</a></li>
                        </ul>
                    </span>
                        -->
                </li>
               
            </ul>
        </div>

    </div>

    <div class="header_bottom_right">
        <div class="slider">
            <div id="slider">
                <div id="mover">
                    
                    <div id="slide-1" class="slide">
                        <div class="slider-img">
                            <img src="~/img/imgSide/slide_5.jpg" alt="learn more" />
                        </div>
                        <!--
                        <div class="slider-text">
                            <h1>Clearance<br><span>SALE</span></h1>
                            <h2>UPTo 20% OFF</h2>
                            <div class="features_list">
                                <h4>
                                    Get to Know More About Our Memorable Services Lorem Ipsum is simply dummy
                                    text
                                </h4>
                            </div>
                            <a href="preview.html" class="button">Shop Now</a>
                        </div>
                            -->
                        <div class="clear"></div>
                    </div>

                    <div class="slide">
                        <!--
                        <div class="slider-text">
                            <h1>Clearance<br><span>SALE</span></h1>
                            <h2>UPTo 40% OFF</h2>
                            <div class="features_list">
                                <h4>Get to Know More About Our Memorable Services</h4>
                            </div>
                            <a href="preview.html" class="button">Shop Now</a>
                        </div>
                            -->
                        <div class="slider-img">
                            <a href="preview.html"><img src="~/img/imgSide/slide_7.jpg" alt="learn more" /></a>
                        </div>
                        <div class="clear"></div>
                    </div>

                    <div class="slide">
                        <div class="slider-img">
                            <a href="preview.html"><img src="~/img/imgSide/slide_8.jpg" alt="learn more" /></a>
                        </div>
                        <!--
                        <div class="slider-text">
                            <h1>Clearance<br><span>SALE</span></h1>
                            <h2>UPTo 10% OFF</h2>
                            <div class="features_list">
                                <h4>
                                    Get to Know More About Our Memorable Services Lorem Ipsum is simply dummy
                                    text
                                </h4>
                            </div>
                            <a href="preview.html" class="button">Shop Now</a>
                        </div>
                            -->
                        <div class="clear"></div>
                    </div>
                
                </div>
                </div>
            <div class="clear"></div>
        </div>
    </div>
    <div class="clear"></div>
</div>    

